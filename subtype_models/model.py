# -*- coding: utf-8 -*-
"""Replace this file with the real subtype model without changing the API layer."""


class SubtypeClassifier:
    def __init__(self, *args, **kwargs):
        raise RuntimeError(
            "This is only a placeholder. Add the real subtype PyTorch model and set SUBTYPE_ENABLED=true."
        )
